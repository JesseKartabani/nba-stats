import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";

const LeagueLeaders = () => {
  return (
    <SafeAreaView>
      <Text>LeagueLeaders</Text>
    </SafeAreaView>
  );
};

export default LeagueLeaders;

const styles = StyleSheet.create({});
